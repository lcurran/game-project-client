'use strict';
//
// const buildTable = (data) => {
//   for (let i = 0, let max = data.length; i < max; i++) {
//     $('#search-results').append('<tr><td>' + data.games[i].id + '</td><td>' + data.games[i].player_o.email + '</td></tr>');
//   }
// };
//
// module.exports = {
//   buildTable,
// };
